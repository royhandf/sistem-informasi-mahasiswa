
package Frame;

import javax.swing.ImageIcon;

public class Load_Ikon {
    public ImageIcon ImageIcon(String lokasiFile) {
        ImageIcon ikon = new ImageIcon(lokasiFile);
        return ikon;
    }
}
